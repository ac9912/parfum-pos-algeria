import { useState, useEffect } from 'react';\nimport SplashScreen from './components/Splash/SplashScreen';\nimport LoginPage from './components/Auth/LoginPage';\nimport './index.css';\n\ntype AppState = 'splash' | 'login' | 'dashboard';\n\nfunction App() {\n  const [appState, setAppState] = useState<AppState>('splash');\n  const splashDuration = 4000; // 4 seconds\n\n  useEffect(() => {\n    if (appState === 'splash') {\n      const timer = setTimeout(() => {\n        setAppState('login');\n      }, splashDuration);\n\n      return () => clearTimeout(timer);\n    }\n  }, [appState]);\n\n  const handleLoginSuccess = () => {\n    setAppState('dashboard');\n    // TODO: Implement dashboard view\n  };\n\n  return (\n    <div className=\"w-full h-screen\">\n      {appState === 'splash' && <SplashScreen />}\n      {appState === 'login' && <LoginPage onLoginSuccess={handleLoginSuccess} />}\n      {appState === 'dashboard' && (\n        <div className=\"w-full h-screen bg-luxury-black flex items-center justify-center\">\n          <div className=\"text-center\">\n            <h1 className=\"text-4xl font-french font-bold gradient-text mb-4\">\n              Dashboard\n            </h1>\n            <p className=\"text-luxury-white-dark\">\n              Dashboard coming soon...\n            </p>\n          </div>\n        </div>\n      )}\n    </div>\n  );\n}\n\nexport default App;\n