import { useState } from 'react';\nimport { useTranslation } from 'react-i18next';\nimport { motion } from 'framer-motion';\nimport { Eye, EyeOff, Lock, User, LogIn } from 'lucide-react';\nimport PerfumeBottle3D from '../Splash/PerfumeBottle3D';\n\ninterface LoginCardProps {\n  onLogin: (credentials: { username: string; password: string }) => void;\n  isLoading: boolean;\n}\n\nconst LoginCard = ({ onLogin, isLoading }: LoginCardProps) => {\n  const { t, i18n } = useTranslation();\n  const [username, setUsername] = useState('');\n  const [password, setPassword] = useState('');\n  const [showPassword, setShowPassword] = useState(false);\n  const [rememberMe, setRememberMe] = useState(false);\n  const [error, setError] = useState('');\n\n  const isRTL = i18n.language === 'ar';\n\n  const handleSubmit = (e: React.FormEvent) => {\n    e.preventDefault();\n    setError('');\n\n    if (!username || !password) {\n      setError(t('login.errors.invalidCredentials'));\n      return;\n    }\n\n    onLogin({ username, password });\n  };\n\n  const changeLanguage = (lang: string) => {\n    i18n.changeLanguage(lang);\n    localStorage.setItem('language', lang);\n  };\n\n  const containerVariants = {\n    hidden: { opacity: 0, scale: 0.95 },\n    visible: {\n      opacity: 1,\n      scale: 1,\n      transition: {\n        duration: 0.6,\n        ease: 'easeOut',\n      },\n    },\n  };\n\n  const itemVariants = {\n    hidden: { opacity: 0, y: 10 },\n    visible: {\n      opacity: 1,\n      y: 0,\n      transition: {\n        duration: 0.4,\n      },\n    },\n  };\n\n  return (\n    <motion.div\n      className=\"w-full max-w-md\"\n      variants={containerVariants}\n      initial=\"hidden\"\n      animate=\"visible\"\n    >\n      {/* Glass Card Premium */}\n      <div className=\"glass-effect-premium rounded-3xl p-8 shadow-glass-lg\">\n        <motion.div variants={itemVariants} className=\"flex justify-center mb-6\">\n          <div className=\"w-24 h-24 flex items-center justify-center\">\n            <div className=\"absolute inset-0 animate-golden-glow rounded-full blur-xl\" />\n            <div className=\"relative w-20 h-24\">\n              <PerfumeBottle3D />\n            </div>\n          </div>\n        </motion.div>\n\n        {/* Header */}\n        <motion.div variants={itemVariants} className=\"text-center mb-8\">\n          <h1 className={`text-3xl font-french font-bold gradient-text mb-2 ${\n            isRTL ? 'font-arabic' : ''\n          }`}>\n            PARFUM POS\n          </h1>\n          <p className=\"text-luxury-white-dark text-sm\">\n            {t('login.subtitle')}\n          </p>\n        </motion.div>\n\n        {/* Date & Time */}\n        <motion.div variants={itemVariants} className=\"grid grid-cols-2 gap-4 mb-6 text-center text-sm\">\n          <div>\n            <p className=\"text-luxury-gold text-xs uppercase tracking-widest mb-1\">\n              {t('login.language')}\n            </p>\n            <p className=\"text-luxury-white-dark\">\n              {new Date().toLocaleDateString(i18n.language)}\n            </p>\n          </div>\n          <div>\n            <p className=\"text-luxury-gold text-xs uppercase tracking-widest mb-1\">\n              الوقت\n            </p>\n            <p className=\"text-luxury-white-dark font-mono\">\n              {new Date().toLocaleTimeString('ar-SA', {\n                hour: '2-digit',\n                minute: '2-digit',\n                hour12: false,\n              })}\n            </p>\n          </div>\n        </motion.div>\n\n        {/* Language Selector */}\n        <motion.div variants={itemVariants} className=\"flex gap-2 mb-6\">\n          {['ar', 'en', 'fr'].map((lang) => (\n            <button\n              key={lang}\n              onClick={() => changeLanguage(lang)}\n              className={`flex-1 py-2 px-3 rounded-lg text-xs font-semibold transition-all duration-300 ${\n                i18n.language === lang\n                  ? 'bg-luxury-gold text-luxury-black shadow-luxury'\n                  : 'bg-glass-light text-luxury-gold hover:bg-glass-lighter'\n              }`}\n            >\n              {t(`languages.${lang}`)}\n            </button>\n          ))}\n        </motion.div>\n\n        {/* Error Message */}\n        {error && (\n          <motion.div\n            variants={itemVariants}\n            className=\"bg-red-500/20 border border-red-500/50 text-red-300 px-4 py-3 rounded-lg mb-6 text-sm\"\n          >\n            {error}\n          </motion.div>\n        )}\n\n        {/* Form */}\n        <form onSubmit={handleSubmit} className=\"space-y-4\">\n          {/* Username */}\n          <motion.div variants={itemVariants} className=\"relative\">\n            <div className=\"absolute left-3 top-1/2 transform -translate-y-1/2 text-luxury-gold\">\n              <User size={18} />\n            </div>\n            <input\n              type=\"text\"\n              placeholder={t('login.username')}\n              value={username}\n              onChange={(e) => setUsername(e.target.value)}\n              className={`w-full bg-glass-dark/50 backdrop-blur border border-luxury-gold/30 rounded-lg py-3 px-4 pl-10 text-luxury-white placeholder-luxury-white-dark/50 focus:outline-none focus:border-luxury-gold focus:shadow-lg transition-all ${\n                isRTL ? 'text-right' : ''\n              }`}\n              dir={isRTL ? 'rtl' : 'ltr'}\n            />\n          </motion.div>\n\n          {/* Password */}\n          <motion.div variants={itemVariants} className=\"relative\">\n            <div className=\"absolute left-3 top-1/2 transform -translate-y-1/2 text-luxury-gold\">\n              <Lock size={18} />\n            </div>\n            <input\n              type={showPassword ? 'text' : 'password'}\n              placeholder={t('login.password')}\n              value={password}\n              onChange={(e) => setPassword(e.target.value)}\n              className={`w-full bg-glass-dark/50 backdrop-blur border border-luxury-gold/30 rounded-lg py-3 px-4 pl-10 text-luxury-white placeholder-luxury-white-dark/50 focus:outline-none focus:border-luxury-gold focus:shadow-lg transition-all ${\n                isRTL ? 'text-right' : ''\n              }`}\n              dir={isRTL ? 'rtl' : 'ltr'}\n            />\n            <button\n              type=\"button\"\n              onClick={() => setShowPassword(!showPassword)}\n              className={`absolute ${isRTL ? 'left-4' : 'right-4'} top-1/2 transform -translate-y-1/2 text-luxury-gold hover:text-luxury-gold-light transition-colors`}\n            >\n              {showPassword ? <EyeOff size={18} /> : <Eye size={18} />}\n            </button>\n          </motion.div>\n\n          {/* Remember Me */}\n          <motion.div variants={itemVariants} className=\"flex items-center justify-between\">\n            <label className=\"flex items-center gap-2 cursor-pointer\">\n              <input\n                type=\"checkbox\"\n                checked={rememberMe}\n                onChange={(e) => setRememberMe(e.target.checked)}\n                className=\"w-4 h-4 rounded accent-luxury-gold\"\n              />\n              <span className=\"text-sm text-luxury-white-dark\">{t('login.rememberMe')}</span>\n            </label>\n            <a href=\"#\" className=\"text-sm text-luxury-gold hover:text-luxury-gold-light transition-colors\">\n              {t('login.forgotPassword')}\n            </a>\n          </motion.div>\n\n          {/* Sign In Button */}\n          <motion.button\n            variants={itemVariants}\n            type=\"submit\"\n            disabled={isLoading}\n            className=\"w-full bg-gradient-to-r from-luxury-gold to-luxury-gold-light text-luxury-black font-bold py-3 rounded-lg hover:shadow-luxury transition-all duration-300 disabled:opacity-50 flex items-center justify-center gap-2 group\"\n            whileHover={{ scale: 1.02 }}\n            whileTap={{ scale: 0.98 }}\n          >\n            {isLoading ? (\n              <div className=\"flex items-center gap-2\">\n                <div className=\"w-4 h-4 border-2 border-luxury-black border-t-transparent rounded-full animate-spin\" />\n                <span>{t('login.signIn')}...</span>\n              </div>\n            ) : (\n              <>\n                <span>{t('login.signIn')}</span>\n                <LogIn size={18} className=\"group-hover:translate-x-1 transition-transform\" />\n              </>\n            )}\n          </motion.button>\n\n          {/* Additional Options */}\n          <motion.div variants={itemVariants} className=\"grid grid-cols-2 gap-3 pt-4 border-t border-luxury-gold/10\">\n            <button\n              type=\"button\"\n              className=\"text-sm text-luxury-gold-light hover:text-luxury-gold bg-glass-light rounded-lg py-2 transition-all hover:bg-glass-lighter\"\n            >\n              {t('login.backup')}\n            </button>\n            <button type=\"button\" className=\"text-sm text-luxury-gold-light hover:text-luxury-gold bg-glass-light rounded-lg py-2 transition-all hover:bg-glass-lighter\">\n              {t('login.language')}\n            </button>\n          </motion.div>\n        </form>\n      </div>\n    </motion.div>\n  );\n};\n\nexport default LoginCard;\n