import { Converter } from 'hijri-converter';\n\nexport const getFormattedDate = (date: Date): string => {\n  return date.toLocaleDateString('ar-SA', {\n    weekday: 'long',\n    year: 'numeric',\n    month: 'long',\n    day: 'numeric',\n  });\n};\n\nexport const getHijriDate = (): string => {\n  const today = new Date();\n  const hijri = Converter.toHijri(today.getFullYear(), today.getMonth() + 1, today.getDate());\n  \n  const hijriMonths = [\n    'محرم',\n    'صفر',\n    'ربيع الأول',\n    'ربيع الثاني',\n    'جمادى الأولى',\n    'جمادى الثانية',\n    'رجب',\n    'شعبان',\n    'رمضان',\n    'شوال',\n    'ذو القعدة',\n    'ذو الحجة',\n  ];\n\n  return `${hijri.hd} ${hijriMonths[hijri.hm - 1]} ${hijri.hy}`;\n};\n\nexport const getHijriDateObject = (date: Date) => {\n  return Converter.toHijri(date.getFullYear(), date.getMonth() + 1, date.getDate());\n};\n